import { createFileRoute, Link } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { SiteShell } from "@/components/site-shell";
import { STORAGE_KEY, calculateTitration, type TitrationState } from "@/lib/titration";
export const Route=createFileRoute("/natijalar")({head:()=>({meta:[{title:"Natijalarim — TajribaLab"},{name:"description",content:"Qurilmada saqlangan virtual laboratoriya natijalari."},{property:"og:title",content:"Natijalarim — TajribaLab"},{property:"og:description",content:"Saqlangan tajriba natijalari va hisobotlar."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Results});
function Results(){const [state,setState]=useState<TitrationState|null>(null);useEffect(()=>{const raw=localStorage.getItem(STORAGE_KEY);if(raw)try{setState(JSON.parse(raw) as TitrationState)}catch{}},[]);const result=state?calculateTitration(state):null;return <SiteShell><main className="mx-auto max-w-5xl px-4 py-12 sm:px-6"><p className="eyebrow">Shu qurilmada</p><h1 className="page-title">Natijalarim</h1>{state&&result?<article className="result-row"><div><span className="status-ready">Davom etmoqda</span><h2>Kislota–ishqor titrlash</h2><p>Oxirgi yangilanish: {new Date(state.updatedAt).toLocaleString("uz-UZ")}</p></div><div className="result-metrics"><span><small>NaOH</small>{state.baseAddedMl.toFixed(2)} ml</span><span><small>pH</small>{result.ph.toFixed(2)}</span></div><Link to="/lab/$id" params={{id:"kislota-ishqor-titrlash"}} className="btn-primary">Davom ettirish</Link></article>:<div className="empty-state"><p>Hali saqlangan tajriba yo‘q.</p><Link to="/laboratoriyalar" className="btn-primary">Laboratoriyani tanlash</Link></div>}</main></SiteShell>}